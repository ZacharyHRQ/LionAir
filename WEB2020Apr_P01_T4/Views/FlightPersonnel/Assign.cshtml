@model WEB2020Apr_P01_T4.Models.FlightPersonnel

@{
    ViewData["Title"] = "Assign";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <link rel="stylesheet" type="text/css" href="~/css/FlightPersonnelEdit.css">
</head>

<h4 class="PageTitle">Edit Staff Record</h4>
<hr />
<div class="registration-form">
    <form asp-action="Assign">
        <fieldset class="FormContent">
            <div class="form-group">
                <label asp-for="StaffID" class="col-sm-3 col-form-label">Flight Captain</label>
                <div class="col-sm-9 col-md-5">
                    <select asp-for="StaffID" asp-items="@ViewData["ScheduleList"]as List<SelectListItem>" class="form-control"></select>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="StaffID" class="col-sm-3 col-form-label">Pilot</label>
                <div class="col-sm-9 col-md-5">
                    <select asp-for="StaffID" asp-items="@ViewData["ScheduleList"]as List<SelectListItem>" class="form-control"></select>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="StaffID" class="col-sm-3 col-form-label">Cabin Crew Leader</label>
                <div class="col-sm-9 col-md-5">
                    <select asp-for="StaffID" asp-items="@ViewData["ScheduleList"]as List<SelectListItem>" class="form-control"></select>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="StaffID" class="col-sm-3 col-form-label">Flight Attendance</label>
                <div class="col-sm-9 col-md-5">
                    <select asp-for="StaffID" asp-items="@ViewData["ScheduleList"]as List<SelectListItem>" class="form-control"></select>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="StaffID" class="col-sm-3 col-form-label">Flight Attendance</label>
                <div class="col-sm-9 col-md-5">
                    <select asp-for="StaffID" asp-items="@ViewData["ScheduleList"]as List<SelectListItem>" class="form-control"></select>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="StaffID" class="col-sm-3 col-form-label">Flight Attendance</label>
                <div class="col-sm-9 col-md-5">
                    <select asp-for="StaffID" asp-items="@ViewData["ScheduleList"]as List<SelectListItem>" class="form-control"></select>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-9 offset-sm-3">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </div>
        </fieldset>
        @Html.HiddenFor(m => m.EmailAddr)
        @Html.HiddenFor(m => m.Status)
    </form>
</div>